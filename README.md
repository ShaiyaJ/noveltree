# Noveltree
## About
Noveltree is a web app that allows writers to organise their written works via a binary tree. Nodes left of the parent come before, nodes right of the parent come after.

This allows writers to make additions to their works in a way that keeps them isolated from some main monolith document.

## Development plans
There are still a few features left:

- Caching last edited document
- "Load recent" menu option
- Dark mode
- PWA support
